# frozen_string_literal: true
source 'https://rubygems.org'

gemspec

gem 'appraisal'
gem 'rubocop'
gem 'rubocop-shopify', "~> 1.0.4", require: false

gem 'rspec'

# For the puma plugin specs. Not a runtime dependency, the plugin only loads
# when an app's puma.rb asks for it.
gem 'puma'
